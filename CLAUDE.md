# CLAUDE.md

**세금맛집 (tax_matzip)** — Claude Code가 매 세션 시작 시 읽는 프로젝트 규칙.

---

## 프로젝트 개요

**세금맛집** — 서울시 간부 공무원이 업무추진비로 다녀간 식당 지도

- 리포명 / 슬러그: `tax_matzip`
- 원본: https://opengov.seoul.go.kr/expense/list
- 공개 대상: 서울시 시장단 및 **4급 부서장 이상** 공무원의 업무추진비
- **수집 범위: 2025년 7월 ~ 2026년**. 2025년 6월 이전은 수집하지 않는다
- 비영리 개인 프로젝트. 상업적 이용 계획 없음
- 참고 사례: https://cham-monimap.com (대전참여연대)

### 기술 스택 (확정)

| 용도 | 선택 |
|---|---|
| DB | Supabase (PostgreSQL) |
| 수집·정제 | Python (배치) |
| **좌표 획득** | **카카오 로컬 API** (주소 검색 → 실패 시 키워드 검색) |
| **지도 표시** | **네이버 Dynamic Map** |
| 웹 | Next.js + Vercel |

**지도와 지오코딩을 다른 회사 것으로 섞어 쓴다.** 흔한 구성이고 문제없다.
두 서비스 모두 WGS84(위경도)를 쓰므로 카카오에서 얻은 좌표를 네이버 지도에 그대로 넘기면 된다.
단, **카카오 REST 응답은 `x`가 경도(lng), `y`가 위도(lat)** 다. 순서를 뒤집지 말 것.

무료 한도는 이 프로젝트 규모에서 근처에도 못 간다 (카카오 주소검색 일 10만건,
네이버 Dynamic Map 월 600만건). 유료 전환을 제안하지 말 것.

### 데이터가 어디에 있는가 — 중요

**첨부 엑셀을 받을 필요가 없다.** 집행내역은 게시글 **본문 HTML 표**에 그대로 있다.
게시글 1건당 요청은 **1회**다.

### 목록은 '연도 + 월'로 조회한다 — 중요

```
https://opengov.seoul.go.kr/expense/list?ym[year]=2026&ym[month]=7&items_per_page=50&page=2
```

**평면 페이지네이션(`?page=N`만 사용)을 쓰지 말 것.** 새 글이 올라오면 목록이 밀려서
누락·중복이 생긴다. 월을 작업 단위로 삼으면 이미 수집한 달의 목록은 변하지 않는다.

- `items_per_page=50` 을 반드시 붙인다 (기본 15 → 요청이 1/3로 줄어든다)
- 목록 1페이지 첫 행의 연번 = **그 달의 총 건수**. 진행률 계산에 쓴다
- 대상: 2025-07 ~ 2026-12 (약 18개월), 최신 월부터 과거 순

---

## 절대 규칙 (위반 금지)

### 1. 정보소통광장 요청 간격 20초를 절대 줄이지 않는다

서울시 공식 공지:

> 일부 사용자가 웹 사이트 수집 프로그램을 과도하게 사용하여 (…) 서버에 과도한 부하를
> 유발하는 사용자는 부득이하게 네트워크단에서 차단이 될 수 있습니다.
> 권고 사항: 한 페이지 수집 후 다음 페이지를 요청하기까지 10초 이상의 간격 (Crawl-delay: 20)

- `DELAY_SEC = 20` 을 낮추자는 제안을 하지 말 것
- 병렬 요청, 비동기 동시 요청, 멀티스레드 수집 금지
- "테스트니까 잠깐만 5초로"도 금지. 차단당하면 프로젝트가 끝난다
- 속도가 필요하면 **수집 범위를 줄이는 것**으로 해결한다
- 이 규칙은 정보소통광장에만 해당한다. 카카오·네이버 API는 정상 속도로 호출해도 된다

### 2. 원본을 절대 버리지 않는다

- `expense_raw_rows.payload` (jsonb) 에 표의 행을 통째로 저장한 뒤 정규화한다
- 정규화 실패 행도 원본은 반드시 적재 (조용히 버리지 말 것)

### 3. 네트워크 없이 개발한다

파서 작업 중 사이트를 호출하지 않는다. `tests/fixtures/` 의 실제 HTML로 테스트한다.
새 케이스가 필요하면 사이트를 긁지 말고 **사용자에게 파일을 요청**할 것.

지오코더도 마찬가지다. 주소 정제 함수(`address_candidates`, `clean_place_name` 등)는
API 호출과 분리되어 있으므로 네트워크 없이 테스트한다.

### 4. 비밀값을 커밋하지 않는다

`.env` 에만 두고 `.gitignore` 등록. 예시는 `.env.example` 에 더미값으로.

```
SUPABASE_DSN=postgresql://...
KAKAO_REST_KEY=...            # 배치 지오코딩용 (서버 사이드)
NEXT_PUBLIC_NAVER_CLIENT_ID=  # 지도 표시용 (브라우저에 노출됨. 도메인 제한 필수)
```

네이버 Client ID는 브라우저에 노출되는 값이다. 콘솔에서 **서비스 URL(도메인) 제한**을
반드시 걸어야 남이 가져다 쓰지 못한다.

### 5. 파괴적 SQL 금지

`drop table`, `truncate`, `delete from` 을 사용자 확인 없이 실행하지 않는다.
스키마 변경은 `db/00N_*.sql` 추가로만.

---

## 사용자 정보

- 커뮤니케이션은 **한국어**로
- 웹 개발은 취미 수준. SQL과 데이터 처리 개념은 능숙 (본업이 ERP/SCM 데이터 통합)
- 프론트엔드 최신 생태계 용어는 풀어서 설명할 것
- 기술 용어를 그대로 쓰지 말고 비전공자도 이해할 수 있게

---

## 표 구조 (본청·사업소·자치구 모두 동일함을 확인)

| # | 열 이름 | 예시 | 처리 |
|---|---|---|---|
| 0 | 연번 | `1` | 무시 |
| 1 | 부서명 | `재난안전실 재난상황관리과` | `dept_name` |
| 2 | 사용일시 | `2026-07-01 12:03` | **분 단위까지 있다** → `timestamp` |
| 3 | 사용장소 | `참숯골(서울시 중구 무교로 16)` | **주소 포함** → 상호명/주소 분리 |
| 4 | 사용목적 | `… 간담회비 지급('26.7.1.)` | `purpose` |
| 5 | 사용금액(원) | `230,000` | `amount` |
| 6 | 사용자 및 인원 | `재난상황관리과장 외 9명` | 원문 보존 + 인원 추출 |
| 7 | 결제방법 | `카드` / `제로페이` | `payment_method` |
| 8 | 비목 | `시책` / `부서` / `기관` | `budget_type` |

### 표 식별 방법

한 페이지에 표가 2~3개 있다. **헤더 시그니처로 찾는다.**

- `['연번','부서명','사용일시']` 로 시작하는 표 = 집행내역
- `등록일` + `담당부서` 를 포함한 표 = 메타 정보
- HWPX 안내문 표 = 무시 (`<th>` 가 없다)

**집행내역 표가 없는 게시글이 있다** (그 달 집행이 없었던 경우).
오류가 아니므로 `parse_status='no_entries'` 로 정상 처리한다.

### 목록 행 구조

```html
<tr>
  <td class="data-num">34</td>
  <td class="data-title"><a href="/expense/36654894">제목</a></td>
  <td class="data-date">2026-08-02</td>
  <td class="date-hit">8</td>
</tr>
```

---

## 파싱 규칙 (샘플 23행 전부 검증 완료)

### 사용장소 분리 — 마지막 괄호쌍이 주소다

```
참숯골(서울시 중구 무교로 16)
  → 상호='참숯골', 주소='서울시 중구 무교로 16'

전설의 우대갈비(서울시청 직영점)(서울시 중구 남대문로9길 40)
  → 상호='전설의 우대갈비(서울시청 직영점)', 주소='서울시 중구 남대문로9길 40'
```

첫 괄호를 쓰면 안 된다. 상호명 자체에 괄호가 들어가는 경우가 있다.

### 인원 추출 — '외 N명'은 +1

```
재난상황관리과장 외 9명            → 10
하광태 외 4인                      → 5      ('인' 표기도 쓰인다)
연구기획과 직원 5명                → 5
재난안전상황실 교대근무직원(16명)  → 16     (괄호 안 숫자)
```

### 서울 밖 사용처가 있다

`고가풍경(경기 고양시 일산서구 대화동 2600)` 같은 사례.
버리지 말고 적재하되, 지도 기본 화면에서는 서울로 필터링한다.

---

## 지오코딩 (카카오)

주소가 함께 공개되므로 주소 검색이 1순위다.

### 2단계 전략

1. **주소 검색** `/v2/local/search/address` — 주소를 단계적으로 단순화하며 재시도
2. **키워드 검색** `/v2/local/search/keyword` — 상호명으로 찾기, 자치구명을 붙여 범위 축소
3. 결과 없음 → `not_found` / 자치구 지정에도 후보 복수 → `ambiguous`

**`ambiguous` 를 임의로 하나 골라 `matched` 로 바꾸지 말 것.** 사람이 확인할 목록이다.

### 주소 정제에서 조심할 것

실제 데이터의 꼬리 패턴과, 잘못 자르기 쉬운 지점:

```
서울시 중구 서소문로 11길 19 LL층
  → '서울특별시 중구 서소문로 11길 19'
  ✗ '서소문로 11' 로 자르면 안 된다 ('로 N길 M' 형태를 통째로 인식해야 함)
  ✗ 'LL층' 에서 'L' 만 지워 '19 L' 이 남으면 안 된다

서울시 종로구 윤보선길 16-1, 1층  → '서울특별시 종로구 윤보선길 16-1'
서울 광진구 광나루로 516 하이웨이마트 → '서울특별시 광진구 광나루로 516'
서초구 매헌로 17                  → '서울특별시 서초구 매헌로 17' (시도 보완)
```

### 캐시

`places.lookup_key` = 상호명+주소에서 공백 제거한 값. 같은 가게를 두 번 조회하지 않는다.
같은 식당이 수백 번 나오므로 이게 없으면 호출량이 수십 배가 된다.

---

## 서비스 톤

- "감시"가 아니라 "기록을 보여준다"는 태도. 카피에 단정적·비난조 표현 금지
- **"인증", "공식", "서울시 선정" 표현 금지** — 서울시가 보증한 것처럼 읽힌다
- 주말·심야 사용 데이터도 단정하지 않는다. 정당한 사유가 있는 경우가 많다

## 개인정보 처리 방침 (사용자 결정)

`사용자 및 인원` 칸에 실명이 포함된 경우가 있다 (`하광태 외 4인`).
**사용자 판단으로 원문 그대로 저장하고 화면에도 표시한다.** 정보공개법에 따라
공개된 공무원 직무 관련 자료이고, 비영리 프로젝트이기 때문이다.

- `expense_entries.participants` 에 원문 저장, 화면에도 그대로 사용
- **마스킹 로직을 임의로 추가하지 말 것**
- 다만 게시글 하단 **담당자 실명은 수집하지 않는다** (집행 당사자가 아니라 게시 담당자)
- Phase 5 에서 정정·삭제 요청 연락 경로를 페이지에 둔다

---

## 디렉터리 구조

```
tax_matzip/
├── CLAUDE.md
├── PLAN.md                  # 진행 상황. 작업 완료 시 반드시 갱신
├── .env / .env.example
├── requirements.txt
├── db/001_schema.sql
├── src/
│   ├── config.py
│   ├── fetcher.py           # HTTP. 20초 지연이 여기 한 곳에만 존재
│   ├── listing.py           # 목록 URL 빌더 + 파서 (월 단위)
│   ├── page_parser.py       # 게시글 HTML → 행. 네트워크 의존 없음
│   ├── loader.py            # Supabase 적재
│   ├── geocoder.py          # 카카오 주소/키워드 검색
│   └── cli.py
├── tests/
│   ├── fixtures/            # 게시글 HTML 5건 + 목록 HTML 1건
│   └── test_*.py
└── web/                     # Next.js + 네이버 지도
```

---

## 명령어

```bash
pytest tests/ -v                        # 파서 테스트 (네트워크 불필요)
python -m src.cli list                  # 36개월 목록 수집
python -m src.cli fetch --limit 10      # 게시글 본문 수집 + 적재
python -m src.cli geocode --limit 100   # 카카오로 좌표 붙이기
python -m src.cli renormalize           # 원본은 두고 정규화만 재실행
python -m src.cli status                # 진행 상황
```

---

## 작업 방식

- 세션 시작 시 `PLAN.md` 를 읽고 현재 Phase를 확인한다
- 작업 완료 시 `PLAN.md` 체크박스를 갱신한다 (시키지 않아도)
- 🧑 항목은 **사용자만 할 수 있는 일**. 대신 하려 하지 말고 필요한 것을 구체적으로 요청한다
- 파서를 바꿀 때는 **먼저 fixture 테스트를 통과시킨 뒤** DB에 반영한다
- 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 한 줄
