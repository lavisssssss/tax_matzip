# PLAN.md

**세금맛집 (tax_matzip)** — 단계별 계획

> Claude Code가 이 파일을 읽고 진행한다. 작업을 마치면 체크박스를 갱신할 것.
> 🧑 = 사용자만 할 수 있는 일 (대신 하지 말고 필요한 것을 요청) / 🤖 = Claude Code 담당

**현재 Phase: 0 잔여 항목 (계정 발급) → 이후 바로 Phase 2**

---

## 확정 사항

| 항목 | 결정 |
|---|---|
| 수집 범위 | **2025년 7월 ~ 2026년** (약 18개월) |
| 데이터 위치 | 게시글 본문 HTML 표 (첨부 엑셀 불필요) |
| 표 구조 | 본청·사업소·자치구 **모두 동일** (9열 고정) |
| 목록 조회 | 연도·월 드롭다운 검색 (평면 페이지네이션 사용 안 함) |
| 개인 실명 | **원문 그대로 저장·표시.** 마스킹하지 않음 |
| DB | Supabase (PostgreSQL) |
| **좌표 획득** | **카카오 로컬 API** (주소 검색 → 실패 시 키워드 폴백) |
| **지도 표시** | **네이버 Dynamic Map** |
| 웹 | Next.js + Vercel |
| 성격 | 비영리 개인 프로젝트 |

> 지도와 지오코딩을 다른 회사 것으로 섞어 쓴다. 둘 다 WGS84라 좌표는 그대로 호환된다.
> 무료 한도(카카오 주소검색 일 10만 / 네이버 지도 월 600만)에 근처도 못 간다.

---

## Phase 0 — 준비

- [x] 🧑 샘플 게시글 HTML 5건 확보
- [x] 🧑 목록 페이지 HTML 확보
- [x] 🧑 자치구 표 구조 동일 확인
- [x] 🧑 지도·지오코딩 스택 결정
- [ ] 🧑 **Supabase 프로젝트 생성** (region: Northeast Asia (Seoul))
      → `.env` 에 `SUPABASE_DSN=`
      (Project Settings > Database > Connection string > **Session pooler**)
- [ ] 🧑 **카카오 REST API 키 발급** → `.env` 에 `KAKAO_REST_KEY=`
      - developers.kakao.com > 내 애플리케이션 > 앱 생성 > 앱 키 > **REST API 키**
      - ⚠️ 카카오맵 무료 쿼터는 **개발자 계정 기준 첫 번째로 활성화한 앱**에만 제공된다.
        앱을 여러 개 만들지 말 것
- [ ] 🧑 **네이버 지도 Client ID 발급** → `.env` 에 `NEXT_PUBLIC_NAVER_CLIENT_ID=`
      - ⚠️ **Services > Application Services > Maps** 로 신청해야 무료다.
        구버전 메뉴(AI·NAVER API)로 신청하면 무료 이용량이 없다
      - 대표 계정 1개에만 무료 제공. 콘솔에서 일별/월별 이용량 상한선도 걸어둘 것
      - 서비스 URL(도메인) 제한 필수 (브라우저에 노출되는 키다)
- [x] 🤖 리포 초기화: 디렉터리, `.gitignore`, `requirements.txt`, `.env.example`
- [x] 🤖 샘플 HTML 6건을 `tests/fixtures/` 로 배치

---

## Phase 1 — 파서 (네트워크 불필요)

- [x] 🤖 `page_parser.py` — 샘플 23행 전부 통과
  - [x] 헤더 시그니처로 집행내역 표 식별 / 없으면 `no_entries`
  - [x] 사용장소 → 상호명/주소 분리 (마지막 괄호쌍 규칙, 23/23)
  - [x] 인원 추출 (`외 N명` = +1, `명`/`인` 혼용, 괄호 안 숫자, 23/23)
  - [x] 제목 → 연월/기관/부서/집행유형, 메타 표 → 등록일·담당부서
- [x] 🤖 `listing.py` — 실제 목록 HTML로 검증
  - [x] `ym[year]`/`ym[month]`/`items_per_page=50` URL 생성
  - [x] 목록 행 파싱, 연번 최댓값으로 그 달 총 건수 계산
- [x] 🤖 `geocoder.py` 주소 정제 로직 — 실제 주소 9종으로 검증
  - [x] `서소문로 11길 19 LL층` → `서소문로 11길 19` (11로 잘리지 않음)
  - [x] `서초구 매헌로 17` → 시도 보완
  - [x] `㈜이마트 양재점` → `이마트 양재점`
- [x] 🤖 `tests/test_page_parser.py`, `test_listing.py`, `test_geocoder.py` 회귀 테스트

**완료 기준: fixture 전체 통과. 샘플 5건 = 23행 / 합계 2,589,710원**

---

## Phase 2 — DB 적재

- [x] 🧑 Supabase SQL Editor 에서 `db/001_schema.sql` 실행
      (`crawl_months` 에 36개월이 자동 생성된다)
- [x] 🤖 `loader.py`: 원본 행 + 정규화 행 upsert
- [x] 🤖 fixture 5건을 실제로 Supabase에 적재
- [x] 🤖 `cli.py status` / `cli.py renormalize`
- [x] 🧑 Supabase 대시보드에서 확인 (23행 / 2,589,710원)

**완료 기준: 같은 명령을 두 번 돌려도 행 수가 늘지 않는다 (멱등성)**

---

## Phase 3 — 크롤러 (정보소통광장, 20초 규칙 적용 구간)

### 3-1. 목록 수집 먼저 완주한다

본문 수집 전에 **36개월 목록만** 먼저 전부 훑는다.
총 몇 건인지 정확히 알아야 본문 수집 시간을 제대로 견적낼 수 있다.

- [x] 🤖 `fetcher.py`: 20초 지연이 **한 곳에만** 존재하도록 (`get_with_retry` 추가)
- [ ] 🧑 소규모 시험 — **2026년 7월 1개월만** (`python -m src.cli list --year 2026 --month 7`)
- [x] 🤖 36개월 순회 (최신 → 과거), `crawl_months.status` 갱신 (`cmd_list` 구현)
- [ ] 🧑 **총 건수 확인 후 본문 수집 시간 재산정**

> 한 달이 300건이라면 6페이지 × 36개월 = 216회 × 20초 ≈ **72분**. 부담 없다.

### 3-2. 본문 수집

- [ ] 🧑 10건만 시험 (`python -m src.cli fetch --limit 10`)
- [x] 🤖 `pending` 상태만 이어서 처리 (중단/재개) (`cmd_fetch` 구현)
- [x] 🤖 실패 재시도 (지수 백오프, 최대 3회) (`get_with_retry` 구현)
- [ ] 🧑 오래 돌릴 방법 결정 (노트북 / 라즈베리파이 / 저가 VPS)

게시글 1건 = 요청 1회 × 20초. 3,000건 ≈ 17시간, 9,000건 ≈ 50시간.
정확한 건수는 3-1이 끝나야 나온다.

**완료 기준: 중단 후 재시작해도 중복 없이 이어진다**

---

## Phase 4 — 지오코딩 (카카오)

정보소통광장의 20초 규칙과 무관하다. 정상 속도로 호출해도 된다.

- [x] 🤖 `geocoder.py` API 호출부 연결 (정제 로직은 완료)
- [x] 🤖 `places.lookup_key` 로 중복 조회 방지
- [x] 🤖 1단계 주소 검색 → 2단계 키워드 폴백 → `not_found` / `ambiguous`
- [x] 🤖 `cmd_geocode` 구현 및 실행 중 (백그라운드)
- [x] 🤖 not_food 처리: 쿠팡, 서울시청, (주) 포함, 마트 등 비식당 일괄 제외
- [x] ✅ **중복 통합 완료** (place_name + sigungu 기준, expense_entries 리다이렉트)
      not_food 처리도 완료: 마트, 기프트카드, 은행, 주식회사, 청과, 매점 등 다수
- [ ] 🤖 `ambiguous` 건 목록 확인 후 보정
- [ ] 🤖 지오코딩 완료 후 최종 매칭률 보고

| 자동 매칭률 | 대응 |
|---|---|
| 85% 이상 | 그대로 진행 (주소 기반이므로 여기 나올 가능성이 높다) |
| 60~85% | 방문 횟수 상위 200곳만 수동 보정 |
| 60% 미만 | 주소 정제 로직 재검토 |

> `ambiguous` 를 임의로 `matched` 로 승격시키지 말 것.

---

## Phase 5 — 웹페이지 (Next.js + 네이버 지도)

- [ ] 🤖 Next.js 프로젝트 생성, Supabase 클라이언트 연결
- [ ] 🤖 네이버 지도 SDK 로드 + 마커 표시 (방문 횟수에 따라 크기 차등)
      - 카카오 좌표를 그대로 사용. `x`=경도, `y`=위도 순서 주의
- [ ] 🤖 마커 클릭 → 방문 기관, 횟수, 총액, 1인당 평균, 사용자 및 인원
- [ ] 🤖 필터: 기관별 / 자치구별 / 기간 / 금액대 / 결제방법 / 비목
- [ ] 🤖 랭킹 페이지 (`v_place_ranking`)
- [ ] 🤖 "1인당 금액" 정렬 — 가성비 관점 콘텐츠
- [ ] 🧑 도메인 확보 (`taxmatzip.com` / `tax-matzip.kr`)
- [ ] 🧑 네이버 콘솔에 배포 도메인 등록 (안 하면 지도가 안 뜬다)
- [ ] 🧑 Vercel 배포

### 브랜딩 (확정)

- 서비스명: **세금맛집**
- 부제: 서울시 간부 공무원이 업무추진비로 다녀간 식당 지도
- 슬러그: `tax_matzip`

### 공개 전 점검 (🧑)

- [ ] 출처 표기 (서울 정보소통광장) + 원문 링크
- [ ] 데이터 기준 시점 명시
- [ ] **정정·삭제 요청 연락 경로** (이메일 정도면 충분)
- [ ] "인증·공식·선정" 표현을 쓰지 않았는가
- [ ] 주말·심야 데이터를 단정적으로 표현하지 않았는가
- [ ] 네이버 Client ID 에 도메인 제한이 걸려 있는가

---

## Phase 6 — 운영

- [ ] 🤖 월 1회 증분 수집 — `crawl_months` 에 새 달 추가 후 그 달만 수집
- [ ] 🤖 GitHub Actions 스케줄 실행
- [ ] 🤖 신규 장소 자동 지오코딩

---

## 지금 당장 할 일

1. 🧑 Supabase 프로젝트 (5분)
2. 🧑 카카오 REST 키 (5분) — 앱은 **하나만** 만들 것
3. 🧑 네이버 Maps Client ID (10분) — **Application Services > Maps** 메뉴로
4. 🤖 리포 초기화 → 3개 모듈 이식 → 테스트 작성 → Phase 2

파서·목록·주소정제는 이미 검증이 끝났다. 계정 3개만 만들면 바로 달릴 수 있다.
